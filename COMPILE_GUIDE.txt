═══════════════════════════════════════════════════════════════════════════════
                      SimpleOS - 编译和运行指南
═══════════════════════════════════════════════════════════════════════════════

你已拥有完整的 SimpleOS 项目。现在可以开始编译和运行！

═══════════════════════════════════════════════════════════════════════════════
                        🚀 快速编译 (3 个选项)
═══════════════════════════════════════════════════════════════════════════════

选项 1️⃣: 完全自动编译 ⭐ 推荐
──────────────────────────────────────

这个选项会自动:
✓ 安装 WSL 2
✓ 安装 Ubuntu
✓ 安装所有编译工具
✓ 编译 SimpleOS
✓ 可选：立即运行 OS

步骤:
  1. 打开 PowerShell (右键 → 以管理员身份运行)
  2. cd c:\Users\mfanq\OneDrive\Desktop\cos
  3. .\build-and-compile.ps1
  
  等待脚本完成...
  
  如果需要重启:
    - 重启电脑后再次运行 .\build-and-compile.ps1


选项 2️⃣: 手动 WSL 设置
──────────────────────────────────────

适用于想手动控制每个步骤的用户。

步骤:
  1. PowerShell (管理员):
     wsl --install -d Ubuntu
  
  2. 重启电脑
  
  3. 打开 Ubuntu (搜索栏搜索 "Ubuntu") 或运行:
     wsl
  
  4. 进入项目目录:
     cd /mnt/c/Users/mfanq/OneDrive/Desktop/cos
  
  5. 运行编译脚本:
     bash compile.sh
  
  6. 选择是否立即运行 OS (按 y 或 n)


选项 3️⃣: 直接使用 Makefile (WSL 已安装)
──────────────────────────────────────

这是最传统的方式，一次一个命令。

步骤:
  1. wsl
  2. cd /mnt/c/Users/mfanq/OneDrive/Desktop/cos
  3. make clean all              (编译)
  4. make run                    (运行)

其他命令:
  make debug                     (调试模式)
  make clean                     (清理)

═══════════════════════════════════════════════════════════════════════════════
                            📋 文件说明
═══════════════════════════════════════════════════════════════════════════════

新创建的文件:

  build-and-compile.ps1     自动编译脚本 (Windows PowerShell)
  compile.sh                自动编译脚本 (WSL/Linux)
  
已有的文件:

  Makefile                  GNU Make 构建配置
  setup.sh                  WSL 自动设置脚本
  build.ps1                 PowerShell 构建脚本
  build.py                  Python 构建脚本

═══════════════════════════════════════════════════════════════════════════════
                          🎯 预期输出
═══════════════════════════════════════════════════════════════════════════════

编译成功后，你会看到:

  ✅ 编译成功!
  
  📊 编译结果:
    • 引导加载程序: build/boot.bin
    • 内核 ELF: build/kernel.elf
    • 磁盘镜像: build/os.img

然后脚本会询问:
  
  是否要现在运行 SimpleOS? (y/n)

按 y 会启动 QEMU，你会看到:

  Welcome to SimpleOS!
  Kernel is running...
  
  System Information:
  - Architecture: x86
  - Memory: Detecting...
  - CPU: Pentium-class or higher
  
  Press Ctrl+C to halt (in emulator).

═══════════════════════════════════════════════════════════════════════════════
                        🆘 常见问题
═══════════════════════════════════════════════════════════════════════════════

问题: "命令未找到" 或 "无法识别为 cmdlet"
解决: 
  • 确保在正确的目录: cd c:\Users\mfanq\OneDrive\Desktop\cos
  • 使用正确的路径 (路径中有空格需要用引号)
  • 如果是 bash 命令，需要在 WSL 中运行: wsl

问题: PowerShell 拒绝执行脚本
解决:
  • 以管理员身份运行 PowerShell
  • 运行: Set-ExecutionPolicy -ExecutionPolicy RemoteSigned

问题: WSL 安装失败
解决:
  • 检查 Windows 版本 (需要 2004 或更新)
  • 确保启用了虚拟化 (BIOS 设置)
  • 重启电脑后重试

问题: 编译错误
解决:
  • 查看错误信息
  • 运行 make clean 清理后重新编译
  • 确保项目目录完整

问题: QEMU 无法启动
解决:
  • 检查 build/os.img 是否存在
  • 重新编译: make clean && make all
  • 确保 QEMU 已安装

═══════════════════════════════════════════════════════════════════════════════
                          💡 提示和技巧
═══════════════════════════════════════════════════════════════════════════════

1. 了解编译过程:
   - boot.asm 被编译成 boot.bin (512 字节)
   - kernel.c + kernel_entry.asm 被编译链接成 kernel.elf
   - kernel.elf 被转换成 kernel.bin
   - boot.bin 和 kernel.bin 被合并成 os.img (可启动镜像)

2. 修改代码后:
   - 编辑 kernel/kernel.c (主要内核代码)
   - 或编辑 boot/boot.asm (引导加载程序)
   - 运行 make clean all 重新编译
   - 运行 make run 测试

3. 调试:
   - make debug 启动 QEMU + GDB 服务
   - 在另一个终端: gdb build/kernel.elf
   - (gdb) target remote localhost:1234
   - (gdb) break kernel_main
   - (gdb) continue

4. 查看编译详情:
   - Makefile 中定义了所有构建步骤
   - 每个命令都有注释说明
   - 可以单独运行每一步

═══════════════════════════════════════════════════════════════════════════════
                         📊 构建过程详解
═══════════════════════════════════════════════════════════════════════════════

make clean all 执行以下步骤:

1. make clean
   ├─ 删除 build 目录
   └─ 创建新的 build 目录

2. make all (编译)
   ├─ 汇编 boot.asm → boot.bin (512 字节)
   │  命令: nasm -f bin boot/boot.asm -o build/boot.bin
   │
   ├─ 汇编 kernel_entry.asm → kernel_entry.o
   │  命令: nasm -f elf32 kernel/kernel_entry.asm -o build/kernel_entry.o
   │
   ├─ 编译 kernel.c → kernel.o
   │  命令: i686-elf-gcc -ffreestanding ... -c kernel/kernel.c
   │
   ├─ 链接 → kernel.elf
   │  命令: i686-elf-ld -m elf_i386 -Ttext 0x1000 ...
   │
   ├─ 提取二进制 → kernel.bin
   │  命令: i686-elf-objcopy -O binary kernel.elf kernel.bin
   │
   └─ 创建磁盘镜像 → os.img (1.44 MB 软盘)
      • 创建空镜像
      • 写入 boot.bin 到偏移 0
      • 写入 kernel.bin 到偏移 1024 (sector 2)

═══════════════════════════════════════════════════════════════════════════════
                        ✅ 编译检查清单
═══════════════════════════════════════════════════════════════════════════════

在开始编译前:

  ☐ 项目目录完整 (boot/, kernel/, src/ 等都存在)
  ☐ 所有源文件都在正确的位置
  ☐ README.md 或 QUICK_REFERENCE.txt 已阅读 (可选)

编译过程:

  ☐ 选择编译方式 (自动 / 手动 / Makefile)
  ☐ 按照步骤操作
  ☐ 等待编译完成

编译后:

  ☐ 看到 "编译成功!" 消息
  ☐ build 目录下有 boot.bin, kernel.elf, kernel.bin, os.img
  ☐ os.img 文件大小为 1,474,560 字节 (1.44 MB)

运行测试:

  ☐ 运行 make run (或选择运行 OS)
  ☐ QEMU 窗口打开
  ☐ 看到 "Welcome to SimpleOS!" 消息
  ☐ 查看系统信息输出

═══════════════════════════════════════════════════════════════════════════════
                        🎉 下一步
═══════════════════════════════════════════════════════════════════════════════

1. 编译 SimpleOS
   运行: .\build-and-compile.ps1 (推荐)
   或按照上面的任意方式编译

2. 在 QEMU 中看到欢迎信息

3. 修改代码并重新编译
   • 编辑 kernel/kernel.c
   • 运行 make clean all
   • 运行 make run 测试

4. 添加新功能
   • 实现键盘中断
   • 添加内存管理
   • 实现进程调度
   • 参考 README.md 了解详情

═══════════════════════════════════════════════════════════════════════════════

现在就开始吧！运行: .\build-and-compile.ps1

═══════════════════════════════════════════════════════════════════════════════
